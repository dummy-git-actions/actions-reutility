name: command
on:
  workflow_call:
jobs:
  execute-command:
    runs-on: ubuntu-latest
    steps:
      - name: corriendo commands
        run: |
          pwd
          ls -la
          mkdir ./demo
          cd ./demo
          pwd
          node init -y
          npm install react        